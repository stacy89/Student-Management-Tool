<%= flash[:errors]%><br>

<% @student_nonpicked.each do |student| %>
  <%= link_to student.name, student_path(student.id) %><br>
  <%= student.phase %><br>
  <%= student.gender %><br>
  <%= student.gpa %><br>
  <%= student.num_detentions %><br>
  <%= link_to "Add student", assign_student_to_teacher_path(student.id), method: :put %><br>
 <% end %>
